apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Release.Name  }}
  labels:
    app: {{ .Release.Name  }}
    release: {{ .Release.Name }}
spec:
  suspend: false
  concurrencyPolicy: Forbid
  schedule: "{{ .Values.schedule }}"
  successfulJobsHistoryLimit: 10
  failedJobsHistoryLimit: 10
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            app: {{ .Release.Name }}
            release: {{ .Release.Name }}
        spec:
          restartPolicy: Never
          serviceAccountName: {{ .Release.Name  }}
          containers:
            - name: {{ .Release.Name }}
              image: "{{ .Values.image }}"
              command: 
              - "sh"
              - "-c"
              - "kubectl get pod | grep -E '{{ .Values.podStatusesToCleanUp }}' | awk '{print $1}' | xargs -r kubectl delete pod -n {{ .Values.namespace }}"
